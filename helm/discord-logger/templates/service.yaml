apiVersion: v1
kind: Service
metadata:
  name: {{ include "discord-logger.fullname" . }}
  labels:
    {{- include "discord-logger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "discord-logger.selectorLabels" . | nindent 4 }}
